Please run the following commands:
pip install -r requirements.txt to start.
main.py <ip_address_to_scan>

This program was developed using python 3.11, please make sure you're running the same
(some APIs do not exist before that version).